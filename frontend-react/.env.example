# Base URL of the Laravel API (no trailing slash, no /api/v1 suffix).
# Local dev default (used automatically if this var is unset): http://127.0.0.1:8000
# Production example:
VITE_API_BASE_URL=https://api.yourdomain.com
