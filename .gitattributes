# Force LF line endings for files that run inside Linux containers.
# A CRLF shell script fails immediately in Docker with a
# "no such file or directory" error on its shebang line.
*.sh text eol=lf
Dockerfile text eol=lf
*.conf text eol=lf
